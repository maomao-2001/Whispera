-r requirements-mem0.txt

# Whispera requires the official GPU build of PyTorch.
# Do not replace this with a CPU-only torch in the default setup.
--extra-index-url https://download.pytorch.org/whl/cu126

fastapi==0.135.1
uvicorn==0.41.0
websockets==16.0
numpy==1.26.4
onnxruntime==1.23.2
funasr==1.3.1
addict==2.4.0
modelscope==1.37.0
librosa==0.11.0
soundfile==0.13.1
torch==2.6.0+cu126
huggingface_hub==1.22.0
einops==0.8.2
transformers==5.13.0
inflect==7.5.0
wetext==0.1.4
safetensors==0.8.0
# The repo currently vendors a local `torchaudio/` package in the workspace,
# so pip `torchaudio` is not added here for now.
# If the vendored `torchaudio/` directory is removed later, add:
# torchaudio==2.6.0+cu126
