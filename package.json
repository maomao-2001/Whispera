{
  "name": "Whispera",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix electron-app",
    "setup:assets": "python scripts/download_assets.py",
    "verify:assets": "python scripts/download_assets.py --verify-only"
  }
}
